[build]
builder = "DOCKERFILE"
dockerfilePath = "services/chat-api/Dockerfile"

[deploy]
preDeployCommand = "alembic -c db/alembic.ini upgrade head"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
